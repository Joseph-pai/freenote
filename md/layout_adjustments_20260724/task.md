# Task: Layout Adjustments (Calendar Height & Resizers)

- `[x]` 備份需要修改的文件
- `[x]` 修正日曆佈局，使其適應剩餘高度 (`page.tsx`, `responsive.css`)
- `[x]` 在 `notes/page.tsx` 新增列表區塊的手動分隔條
- `[x]` 在 `messages/page.tsx` 新增列表區塊的手動分隔條
- `[ ]` 完成後詢問使用者是否保留或刪除 md 檔案
